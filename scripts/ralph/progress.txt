# Ralph Progress Log
Started: Wed Apr 22 23:42:15 CST 2026
---

## Codebase Patterns
- Use `electron-vite` for building Electron app with React
- CSS imports need `env.d.ts` with module declaration for TypeScript
- Use `ELECTRON_MIRROR` env var if npm install fails on electron binary download
- TypeScript project references require `composite: true` in referenced configs
- Database files should be stored in `app.getPath('userData')` for cross-platform compatibility
- Use electron-store with encryptionKey for secure settings storage
- Renderer type declarations must be self-contained (cannot import from main process)
- Use `browserPool.acquire()` / `browserPool.release()` for all Puppeteer operations
- Use `throttledFetch(domain, url, options)` for all HTTP requests to avoid rate limits
- Query modules: API first, web scraping fallback; use try/finally for browser pool
- IPC registration pattern: create handler in src/main/ipc/, register in main index.ts, expose in preload, type in renderer
- All new IPC methods must be added to: preload/index.ts, renderer/src/electron-api.d.ts

## Wed Apr 22 23:50:00 CST 2026 - US-001
- Initialized Electron + React + TypeScript project scaffold using electron-vite
- Created two-panel placeholder layout (left input panel, right results panel)
- Added dark mode support via CSS variables and prefers-color-scheme
- Added scripts: dev (hot reload), build, preview, typecheck, lint
- Files changed:
  - package.json, package-lock.json (dependencies)
  - electron.vite.config.ts (build config)
  - tsconfig.json, tsconfig.node.json, tsconfig.web.json (TypeScript configs)
  - src/main/index.ts (Electron main process)
  - src/preload/index.ts (preload script with contextBridge)
  - src/renderer/* (React app with App.tsx, App.css, index.css, main.tsx, env.d.ts)
  - .gitignore
- **Learnings for future iterations:**
  - electron-vite uses vite@7.x, so plugins must be compatible (use @vitejs/plugin-react@5.x, not 6.x)
  - Dev server runs renderer on http://localhost:5173/ with hot reload
  - Main/preload scripts are built to out/main and out/preload directories
  - Renderer assets are built to out/renderer/
  - Electron binary download can timeout; use ELECTRON_MIRROR="https://npmmirror.com/mirrors/electron/"
---

## Wed Apr 23 00:10:00 CST 2026 - US-002
- Added SQLite database with better-sqlite3 for local persistence
- Created database module with initDatabase/closeDatabase functions
- Implemented auto-migration for queries and results tables
- Database stored in user data directory for cross-platform compatibility
- Files changed:
  - package.json, package-lock.json (added better-sqlite3)
  - src/main/database/index.ts (database module)
  - src/main/index.ts (integrated database lifecycle)
  - .gitignore (added *.tsbuildinfo)
- **Learnings for future iterations:**
  - Use app.getPath('userData') for database file location
  - Enable WAL mode for better concurrent read/write performance
  - Use IF NOT EXISTS for migrations to be idempotent
  - Foreign key constraints require PRAGMA foreign_keys = ON
---

## Wed Apr 22 24:00:00 CST 2026 - US-003
- Added electron-store with encryption for secure settings storage
- Implemented typed Settings interface with discogsToken, ebayClientId, ebayClientSecret, cookies
- Created IPC handlers: getSettings, setSetting, deleteSetting
- Added TypeScript type declarations for window.electronAPI in renderer
- Files changed:
  - package.json, package-lock.json (added electron-store)
  - src/main/settings/index.ts (settings module)
  - src/main/ipc/settings.ts (IPC handlers)
  - src/main/index.ts (integrated settings IPC)
  - src/preload/index.ts (exposed settings API)
  - src/renderer/src/electron-api.d.ts (type declarations)
- **Learnings for future iterations:**
  - electron-store supports encryptionKey for secure storage
  - Renderer needs separate type declaration file since it cannot import from main process
  - IPC handlers should be registered before window creation
---

## Wed Apr 23 00:30:00 CST 2026 - US-004
- Set up Puppeteer stealth scraping engine with browser pool
- Implemented dynamic fingerprinting: random UA, viewport, WebGL vendor
- Created browser pool with acquire/release pattern (max 2 concurrent)
- Applied puppeteer-extra-plugin-stealth for anti-bot detection bypass
- Files changed:
  - package.json, package-lock.json (added puppeteer packages)
  - src/main/browser/fingerprint.ts (fingerprint generation)
  - src/main/browser/pool.ts (browser pool manager)
  - src/main/browser/index.ts (exports)
  - src/main/index.ts (integrated browser pool lifecycle)
- **Learnings for future iterations:**
  - Use puppeteer-extra with stealth plugin for anti-scraping bypass
  - Browser pool should be closed on app quit to prevent zombie processes
  - Fingerprinting includes UA, viewport, and WebGL vendor/renderer spoofing
---

## Wed Apr 23 00:45:00 CST 2026 - US-005
- Added per-domain throttle queue with randomized 2-6s delay
- Implemented exponential backoff on 429 responses (2s, 4s, 8s, then fail)
- Error message indicates manual intervention when retries exhausted
- Exposed getThrottleStatus via IPC
- Files changed:
  - src/main/throttle/index.ts (throttle module)
  - src/main/index.ts (integrated throttle IPC)
  - src/preload/index.ts (exposed getThrottleStatus)
  - src/renderer/src/electron-api.d.ts (added ThrottleStatus type)
- **Learnings for future iterations:**
  - Use `throttledFetch(domain, url, options)` for all HTTP requests from scraping modules
  - Domain is extracted from URL for per-domain throttling
  - Backoff state is tracked per-domain for retry management
---

## Wed Apr 23 01:00:00 CST 2026 - US-006
- Implemented Discogs query module with API + web fallback
- Primary: call Discogs API with user token for search
- Fallback: stealth-scrape Discogs search page on API failure
- Extract name, artist, price range, cover URL, link from results
- Files changed:
  - src/main/queries/types.ts (QueryResult types)
  - src/main/queries/discogs.ts (Discogs query implementation)
- **Learnings for future iterations:**
  - Query modules follow pattern: API first, web scraping fallback
  - Use getSetting('discogsToken') and getSetting('cookies')?.discogs for credentials
  - Browser pool acquire/release must be in try/finally block
---

## Wed Apr 23 01:20:00 CST 2026 - US-007
- Implemented eBay query module with OAuth + web fallback
- Primary: call eBay Browse API with OAuth credentials
- Access token caching with expiry management
- Fallback: stealth-scrape eBay search page
- Files changed: src/main/queries/ebay.ts

## Wed Apr 23 01:30:00 CST 2026 - US-008 & US-009
- Implemented Kojima Rokuon and Mercari query modules
- Both are scraping-only (no public APIs)
- Handle Japanese price formats and UTF-8 encoding
- Files changed: src/main/queries/kojima.ts, src/main/queries/mercari.ts

## Wed Apr 23 01:45:00 CST 2026 - US-010
- Built query orchestrator with parallel execution
- Accept array of up to 10 catalog numbers
- Query all 4 platforms in parallel per catalog number
- Control concurrency: max 3 catalog numbers simultaneously
- Emit IPC events for progress tracking
- Save all results to SQLite after each catalog number completes
- Files changed: src/main/orchestrator/index.ts, src/main/ipc/orchestrator.ts

## Wed Apr 23 02:00:00 CST 2026 - US-011
- Built input panel UI for catalog numbers
- Textarea for catalog numbers (one per line or comma-separated)
- Validation for empty or more than 10 entries
- Search button triggers IPC call to query orchestrator
- Dark mode support via CSS variables
- Files changed: src/renderer/src/App.tsx, src/renderer/src/App.css, src/renderer/src/index.css

## Wed Apr 23 02:15:00 CST 2026 - US-012
- Built settings panel UI
- Accessible via toolbar gear icon
- Input fields for Discogs API token, eBay Client ID/Secret
- Cookie input textarea per platform
- Save button writes settings via IPC; values load on panel open
- Toast notification confirms settings saved
- Files changed: src/renderer/src/Settings.tsx, src/renderer/src/Settings.css

## Wed Apr 23 02:30:00 CST 2026 - US-013
- Built result card component
- Right panel displays result cards grouped by catalog number
- Each card shows catalog number, CD name, artist, cover thumbnail
- Per-platform price range with link
- Cover images load asynchronously with placeholder
- Platform names shown with recognizable label/icon per row
- Files changed: src/renderer/src/App.tsx, src/renderer/src/App.css

## Wed Apr 23 02:45:00 CST 2026 - US-014
- Added price comparison and not-found display
- Lowest price across all platforms highlighted with "Best" badge
- Platforms with no results show "Not Found" badge
- Platforms with errors show "Error" badge with tooltip
- Color-coded border indicators per status
- Files changed: src/renderer/src/App.tsx, src/renderer/src/App.css

## Wed Apr 23 03:00:00 CST 2026 - US-015
- Added query progress indicator
- Progress text shows "X/Y CDs queried" in header
- Spinner and progress area during querying
- Real-time status updates via IPC events

## Wed Apr 23 03:15:00 CST 2026 - US-016
- Built history view
- History tab lists all past query batches
- Clicking history entry loads saved results
- Delete and Clear All buttons
- Results viewable offline
- Files changed: src/main/database/queries.ts, src/main/ipc/history.ts, src/renderer/src/History.tsx

## Wed Apr 23 03:30:00 CST 2026 - US-017
- Implemented Excel export logic with exceljs
- Columns: Catalog Number, Name, Artist, prices per platform, cover URL, links
- Native save dialog for destination path
- Header styling in exported file
- Files changed: src/main/export/index.ts, src/main/ipc/export.ts

## Wed Apr 23 03:40:00 CST 2026 - US-018
- Added export UI button and integration
- Export to Excel button in results view toolbar
- Toast notification confirms export success with file path
- Files changed: src/renderer/src/App.tsx, src/renderer/src/App.css

---
# ALL STORIES COMPLETE - 18/18 passed
